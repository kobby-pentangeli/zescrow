# Node dependencies (Solana agent tests)
node_modules/

# Foundry build output and managed dependencies (Ethereum agent)
agent/ethereum/out/
agent/ethereum/cache/
agent/ethereum/broadcast/
agent/ethereum/dependencies/

.DS_Store
.vscode

# Output from Cargo
target/

# Solana test validator logs
test-ledger

# Escrow workflow files (user copies from chain-specific templates)
deploy/escrow_params.json
deploy/escrow_conditions.json
deploy/escrow_metadata.json

# Recipient keypair generated by deploy scripts
deploy/recipient_keypair.json

# Environment files with secrets
.env